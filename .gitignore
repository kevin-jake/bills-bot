# Secrets / local config — never commit
.env
.env.prod

# SQLite database files (live on a mounted volume, not in the repo)
*.db
*.db-shm
*.db-wal
/data/
/data-docker/

# Go build output
/bin/
/cmd/bot/bot
*.exe

# Test / coverage artifacts
*.out
coverage.*

# Editor / OS noise
.DS_Store
*.swp
.idea/
.vscode/

tmp
